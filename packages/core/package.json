{
  "name": "@bismi/core",
  "version": "1.0.0",
  "description": "Bismi Broilers — Shared business logic (types, config, firebase, cart, slots)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx"
  },
  "dependencies": {
    "firebase": "^11.0.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/uuid": "^10.0.0"
  }
}
